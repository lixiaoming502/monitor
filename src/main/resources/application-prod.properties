server.port=8081
spring.application.name=monitor-service
#logging.file=logs/${spring.application.name}.log

spring.datasource.driverClassName = com.mysql.jdbc.Driver
spring.datasource.url = jdbc:mysql://10.10.193.135:3306/trace_db?useUnicode=true&characterEncoding=UTF-8
spring.datasource.username = trace_user01
spring.datasource.password = zhIfp/MQwxm54a94

###以下为tomcat连接池固有的配置
# Number of ms to wait before throwing an exception if no connection is available.
spring.datasource.tomcat.max-wait=10000
# Maximum number of active connections that can be allocated from this pool at the same time.
spring.datasource.tomcat.max-active=50
spring.datasource.tomcat.max-idle=10
# Validate the connection before borrowing it from the pool.
spring.datasource.tomcat.test-on-borrow=true